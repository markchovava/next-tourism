import { getCookie } from "cookies-next";
import { cookies } from "next/headers";

    
export async function getAuthCookie() {
        const token = getCookie('ENJOYZIM_AUTH_COOKIE', { cookies });
        return token;
}
    
   

